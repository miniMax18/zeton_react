version: 2.1

jobs:
  my-job:
    docker:
      - image: cimg/node:18.20.2
    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
  my-workflow:
    jobs:
      - my-job